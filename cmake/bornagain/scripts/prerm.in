#!/bin/sh
# prerm script for the BornAgain library
# removes created symbolic links


rm -rf @Python_SITELIB@/*bornagain*
rm -rf @Python_SITELIB@/*BornAgain*

rm -rf @CMAKE_INSTALL_PREFIX@/bin/bornagain

rm -rf @CMAKE_INSTALL_PREFIX@/lib/@destination_suffix@
